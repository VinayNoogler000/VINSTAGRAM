<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="author" content="Vinay Tambey">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instagram Clone | Vinay Tambey</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Vinstagram</h1>

    <main>
        <% for(let post of posts) { %>
            <div class="posts">
                <h2 class="username">@<%= post.username %></h2>
                <img src="images/<%= post.image %>" alt="<%= traverseUntil(post.image.split('')).join('') %>">
                <p class="caption"><%= post.caption %></p>

                <div class="links">
                    <a class="detail" href="http://localhost:8080/posts/<%= post.id %>">See in Detail</a>
                    <a class="edit" href="http://localhost:8080/posts/<%= post.id %>/edit">Edit</a>
                </div>
            </div>
        <% } %>

        <a class="newPostLink" href="http://localhost:8080/posts/new"> 
            <img src="icons/plus.svg" alt="add icon"> 
            Create a New Post 
        </a>
    </main>
</body>
</html>